// 실험 전역 설정값. 여기 숫자만 바꾸면 난이도/타이밍을 조절할 수 있다.
const CONFIG = {
  CELL_SIZE: 4,          // 미로 한 칸의 한 변 길이 (world unit)
  WALL_HEIGHT: 3,
  PLAYER_EYE_HEIGHT: 1.6,
  PLAYER_RADIUS: 0.9,    // 충돌 판정용 반지름
  MOVE_SPEED: 3.5,       // unit/sec
  LOOK_SENSITIVITY: 0.0022,
  FOG_NEAR: 1,
  FOG_FAR: 9,             // 이 거리 너머는 안개로 안 보임 (약 2칸 앞) — 시각 정보용
  GOAL_RADIUS: 1.2,       // 이 거리 안에 들어오면 도착 판정
  // 체험 시간을 10분 안에 맞추려고 경로를 29칸까지 줄였었는데, 실제로 플레이해
  // 보니 "헤매는 느낌이 하나도 없이 그냥 진행하다보면 바로 깨진다"는 피드백을
  // 받음 — 랜드마크 0개 조건에서 시간이 의미 있게 늘어나려면 실제로 헤맬 만한
  // 공간이 있어야 하므로, "10분 안에"보다 난이도를 우선해 미로를 한 번 크게
  // 키웠다가(경로 41칸), "난이도를 살짝 낮출 수 있나"는 요청으로 분기 밀도는
  // 유지한 채 경로만 33칸으로 줄이고, "15x15 격자로 제작 가능할까?"는 요청으로
  // 한 번 더 25칸까지 줄였다가, 실제 테스트 참가자 3명 전원이 "너무 쉽다"고
  // 해서 17x17(33칸)로 키우고, "맵 3종을 눈치챈다"는 추가 피드백으로 한 번 더
  // 21x21(41칸)까지 키움(maps.js 참고). 완벽 주행 시간 약 45.7초 기준으로,
  // 헤매는 케이스를 조기 종료시키지 않을 만큼(약 9.5배) 여유를 두고 435초로 설정.
  TRIAL_TIMEOUT_SEC: 435,

  // 재정향(멈춰서 소리 방향 탐색) 판정 기준
  REORIENT_MOVE_EPS: 0.03,       // 이 이하로 움직이면 "정지"로 간주 (unit/frame 누적 아님, 프레임당 이동거리)
  REORIENT_LOOK_MIN: 0.02,       // 이 이상 시선을 돌려야 "탐색 중"으로 간주 (프레임당 각도 누적, 라디안)

  // 청각 랜드마크 (audio.js의 timbre 0/1/2와 순서를 맞춤: 0=나무 타격음, 1=물방울, 2=바람 소리)
  //
  // 거리에 따른 음량 변화(rolloff)를 **유지한다**. 한 번 0으로 없애봤다가 실제
  // 플레이에서 "소리가 아무 반응도 안 해서 의미가 없다"는 판단이 나와 되돌렸다.
  // 음량 변화는 단순한 현실감 요소가 아니라 **방향 판단 자체를 돕는 단서**다 —
  // 몇 걸음 움직였을 때 커지는지 작아지는지가 앞뒤 혼동(HRTF의 고질적 문제)을
  // 푸는 주된 방법이라, 이걸 없애면 신호가 순수해지는 게 아니라 못 쓰게 된다.
  //
  // **감쇠 모델을 inverse -> linear로 교체 (2026-09-16)**: 실제 테스트 참가자
  // 3명 전원이 "소리가 늘 들려서 의미 없이 거슬리기만 한다"는 의견. 원인은
  // Web Audio의 inverse 모델이 스펙상 아무리 멀어도 정확히 0에 도달하지 않고
  // 바닥값에 수렴만 하기 때문(그래서 항상 웅얼웅얼 들림). linear 모델은
  // rolloffFactor=1일 때 maxDistance 밖에서 **정확히 무음**이 되는 게 Web Audio에
  // 기본 내장되어 있다 — "가까워지면 들리기 시작하고, 멀어지면 완전히 사라진다"는
  // on/off 신호가 되어 방향 판단에도 더 뚜렷한 단서를 준다.
  // refDistance(3)~maxDistance(60) 사이에서 선형으로 감쇠하다가 60유닛 밖에서
  // 무음(60유닛 = CELL_SIZE 4로 나누면 직선거리 약 15칸, 걸어서 약 17초 거리).
  // 17x17 미로(대각선 약 90유닛) 기준으로 시뮬레이션해보면 양옆 모서리
  // 랜드마크는 경로의 절반 이상에서 들리고, 미로 밖에 있는 2번(바람 소리)은
  // 경로의 약 12%(시작 근처)에서만 들림 — 항상 들리던 이전과 달리 "들림"
  // 자체가 정보가 된다. maps.js의 2번 랜드마크 배치(8칸 오프셋)는 이 새
  // 모델에서도 진행도 유출이 작게 유지되는지 재확인 후 그대로 둠.
  // 21x21로 확대(2026-09-17)한 뒤에도 60유닛 그대로 둠 — 미로 대각선이
  // 90->113유닛으로 늘어난 뒤 40~105유닛 범위로 재계산해봐도 60유닛이 비슷한
  // 균형(양옆 랜드마크 노출 73%/29%, 바람 소리 17%)을 내서, 미로 크기에 비례해
  // 계속 바꾸는 것보다 절대 거리를 고정해 "가까우면 들림" 감각을 일관되게
  // 유지하는 쪽을 택함(maps.js 랜드마크 배치 주석 참고).
  LANDMARK_ROLLOFF: 1,        // linear 모델에서 1이어야 maxDistance 밖이 정확히 무음(스펙상 최댓값)
  LANDMARK_REF_DISTANCE: 3,   // 이 거리 안에서는 최대 음량
  LANDMARK_MAX_DISTANCE: 60,  // 이 거리(직선 약 15칸) 밖에서는 완전히 무음
  // 전체 음량을 한 곳에서 조절하는 지점. 너무 크거나 작으면 이 값만 바꾸면 된다.
  LANDMARK_MASTER_GAIN: 1.0,

  // 랜드마크에 가까이 갈수록 소리가 떨리는(트레몰로) 근접 확인 효과.
  // 시각적으로 표시하면 "소리로 위치를 추론"하는 조작 자체가 무의미해지므로
  // (docs/02 참고 — 시야 차단 조건에서 청각 채널만으로 승부를 보게 하는 설계),
  // 표시는 청각 채널 안에서만 준다. 랜드마크 좌표 자체가 벽 속이라 실제로
  // 이 거리까지 접근 가능한 지점은 제한적이다.
  LANDMARK_PROXIMITY_FAR: 7,      // 이 거리부터 서서히 떨리기 시작
  LANDMARK_PROXIMITY_NEAR: 2,     // 이 거리 이내에서 최대로 떨림
  LANDMARK_PROXIMITY_DEPTH: 0.35, // 떨림 깊이(0~1, 클수록 또렷하게 떨림)
  LANDMARK_PROXIMITY_RATE: 5.5,   // 떨림 속도 (Hz)

  // 참가자 1~6번에게 자동으로 배정되는 순서표.
  // 각 행: 3회의 본시행에서 (맵, 랜드마크 개수)를 어떤 순서로 배정할지.
  // 맵(0/1/2)×레벨(0/2/3) 각각 3x3 라틴방진을 독립적으로 두 번 반복해서 만들었다
  // (map 회전과 level 회전의 위상을 반복마다 다르게 둬서 맵-레벨 조합이 한쪽으로
  // 쏠리지 않게 함). 검증 스크립트로 확인한 성질:
  //   - 1/2/3번째 시행 자리마다 맵 0/1/2가 각각 정확히 2번씩 나옴
  //   - 1/2/3번째 시행 자리마다 레벨 0/2/3이 각각 정확히 2번씩 나옴
  //   - 맵 하나가 특정 레벨 하나에만 고정되지 않음 (맵마다 서로 다른 레벨 2가지와 짝지어짐)
  // (이전 버전은 map1이 항상 2번째 시행 자리에만 나오는 결함이 있어 다시 짬 — docs/진행상황.md 참고)
  ASSIGNMENT_TABLE: [
    [{ map: 0, level: 2 }, { map: 1, level: 3 }, { map: 2, level: 0 }],
    [{ map: 1, level: 3 }, { map: 2, level: 0 }, { map: 0, level: 2 }],
    [{ map: 2, level: 0 }, { map: 0, level: 2 }, { map: 1, level: 3 }],
    [{ map: 0, level: 3 }, { map: 1, level: 0 }, { map: 2, level: 2 }],
    [{ map: 1, level: 0 }, { map: 2, level: 2 }, { map: 0, level: 3 }],
    [{ map: 2, level: 2 }, { map: 0, level: 3 }, { map: 1, level: 0 }],
  ],

  // 랜드마크 2개 조건일 때, 3개 중 어떤 걸 빼고 쓸지 참가자 슬롯별로 순환시킨다.
  // (매번 같은 랜드마크만 빠지면 그 랜드마크의 위치 효과가 섞여 들어갈 수 있어서)
  LEVEL2_DROP_INDEX_BY_SLOT: [2, 0, 1, 2, 0, 1],
};

// 안전장치: 참가자 한 명의 본시행 3회에 같은 맵이 두 번 배정되면 안 된다는
// 요구가 명시적으로 있었으므로, 각 행이 실제로 맵 0/1/2를 한 번씩만 쓰는지
// 로드 시점에 직접 검증한다. 나중에 표를 수정하다 실수해도 참가자가 플레이해서
// 알아차리기 전에 콘솔에서 바로 걸리게 하기 위함.
(function assertAssignmentTableHasNoDuplicateMaps() {
  CONFIG.ASSIGNMENT_TABLE.forEach((row, i) => {
    const maps = row.map((spec) => spec.map);
    if (new Set(maps).size !== maps.length) {
      throw new Error(
        `ASSIGNMENT_TABLE[${i}]에 같은 맵이 두 번 배정되어 있습니다(${JSON.stringify(maps)}) — ` +
        `참가자가 같은 맵을 두 번 플레이하게 되므로 config.js를 다시 확인하세요.`
      );
    }
  });
})();
